# fanpage

Project is created with:

React-native
Firebase

###
```$ cd ./FanPage
$ yarn install 
$ npx react-native start
If ios
$ npx react-native run-ios
if using android  
$ react-native run-android```
